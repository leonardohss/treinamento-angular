import { environment } from './../environments/environment';

export const API_URL: string = environment.apiUrl;
export const APP_NAME: string = environment.appName;
export const PRODUCTION: boolean = environment.production;
